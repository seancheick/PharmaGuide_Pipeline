"""Sprint E1 report-generation tools.

Modules in this package write structured audit reports (Markdown + JSON)
to ``reports/`` at the repo root. Tools are invocable as ``python3 -m
scripts.reports.<name>`` or directly as scripts.
"""
