# Core runtime and test dependencies for the scripts/ toolchain.
requests>=2.32,<3
rapidfuzz>=3.9,<4
pytest>=9,<10
pytest-xdist>=3.6,<4
pytest-timeout>=2.3,<3   # fail a hung test with a named traceback instead of a multi-hour zombie
jsonschema>=4,<5    # v4 scoring-config rubric validation (Phase 0 config registry)
supabase>=2.0,<3
PyMuPDF>=1.24,<2
Pillow>=10.0,<11
tqdm>=4.66,<5       # progress bars for batch_processor / enrich / score pipeline stages
beautifulsoup4>=4.12,<5
lxml>=5.0,<7
playwright>=1.50,<2
# Dashboard modules are imported by the fast test profile. Keep their runtime
# requirements centralized, and declare pyarrow because the tests import it
# directly rather than relying on Streamlit's transitive dependency.
-r scripts/dashboard/requirements.txt
pyarrow>=17,<26
# Keep the OCR engine/weights reproducible for benchmark fingerprints.
rapidocr-onnxruntime==1.2.3
