# Pipeline Operations

The canonical, code-verified operator runbook is:

[`scripts/PIPELINE_OPERATIONS_README.md`](../scripts/PIPELINE_OPERATIONS_README.md)

This pointer intentionally replaces the former duplicate copy. Maintain the
single canonical document under `scripts/` so operational commands cannot
drift.
