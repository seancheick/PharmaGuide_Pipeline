# Pipeline Maintenance Schedule

The canonical, code-verified maintenance schedule is:

[`scripts/PIPELINE_MAINTENANCE_SCHEDULE.md`](../scripts/PIPELINE_MAINTENANCE_SCHEDULE.md)

This pointer intentionally replaces the former duplicate copy. Maintain the
single canonical document under `scripts/` so clinical and release procedures
cannot drift.
