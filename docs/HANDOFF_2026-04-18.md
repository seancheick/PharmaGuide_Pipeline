# Handoff — 2026-04-18 Session

**From:** Sean + previous Claude session (Opus 4.7, 1M context)
**To:** Next agent picking up the work
**Session goal achieved:** Close Dr. Pham clinical review end-to-end across 6 data files + full Flutter wiring + build the automation roadmap for the next 6 months.
**Status:** Case closed on clinical review; ready to start Phase 1.

---

## What shipped today — already live on `main`

### Pipeline repo (`PharmaGuide_Pipeline`)

Recent commit sequence (newest first):

| Commit  | Title                                                                                    |
| ------- | ---------------------------------------------------------------------------------------- |
| pending | ops README + production validator tests (this handoff's batch)                          |
| 75d0851 | _Flutter_: `ManufacturerViolationsSection` widget (brand_trust_summary rendering)       |
| 03f1ad5 | _Flutter_: added 4 banned_recalled entries missing from prior asset sync                |
| d5dd4df | _Flutter_: wire authored Path C fields into product detail rendering                    |
| 07ee851 | _docs_: monetization timeline + free-tier limits matrix                                 |
| b5cc5d5 | _docs_: Pro-tier monetization layer + ethical gating constraint                         |
| d867317 | _docs_: Phase 4.5 tiered offline architecture (Yuka-style) with researched numbers      |
| 06fc83b | _docs_: Phase 1.5 Safety Alert Short Path + scale notes for 10k→250k                    |
| a07dcd3 | _docs_: automation roadmap + multi-person ops handbook                                  |
| aed7772 | _dashboard_: Path C coverage view + inspector enhancements                              |
| 4b3fca5 | _data_: add `contamination_recall` ban_context (ADR implementation)                     |
| 0377494 | _data_: harmful_additives authored (115) + retire uppercase migration debt (Path B)     |
| e89b628 | _data_: synergy + manufacturer_violations authored (58 + 79)                            |
| 46971b8 | _data_: interaction rules round 2b-full (297 non-severe sub-rules)                      |
| 5a94bde | _data_: interaction rules round 2a (39 pregnancy_lactation blocks)                      |
| 2571e4d | _enricher_: pass alert_headline/alert_body/informational_note through                    |
| 68cdcac | _data_: interaction rules round 1 (77 severe sub-rules)                                 |

### Flutter repo (`Pharmaguide.ai`)

Current state after today: commits through `75d0851` live; `.gitignore` + `pubspec.yaml` have uncommitted Sentry-setup WIP that is NOT this session's work.

### Dr. Pham clinical review — case closed

All 6 target files fully authored, validated in strict mode, tested:

| File                                | Entries / Surfaces                                                | Authored |
| ----------------------------------- | ----------------------------------------------------------------- | -------- |
| `medication_depletions.json`        | 68 entries                                                        | ✅ 100%  |
| `banned_recalled_ingredients.json`  | 143 entries (incl. 9 contamination_recall)                        | ✅ 100%  |
| `ingredient_interaction_rules.json` | 413 surfaces (77 severe + 39 PL + 297 non-severe)                 | ✅ 100%  |
| `harmful_additives.json`            | 115 entries                                                       | ✅ 100%  |
| `synergy_cluster.json`              | 58 entries                                                        | ✅ 100%  |
| `manufacturer_violations.json`      | 79 entries                                                        | ✅ 100%  |

**Total: 876 clinically-authored surfaces.**

End-to-end verified across 4,240 products / 15 brands:
- 8,822 severe interaction warnings → 100% authored
- 9,521 harmful-additive warnings → 100% authored
- 67,435 synergy clusters → 100% authored
- 9 manufacturer violations → 100% authored

### Flutter wiring — case closed

- `InteractionWarning.fromJson` normalizes all 4 authored-field families into unified `alertHeadline`/`alertBody`/`informationalNote` + adds `banContext`.
- Warning cards render authored copy via `displayHeadline`/`displayBody` (fallback to derived).
- `contamination_recall` gets a "Recalled:" banner prefix per the ADR.
- `SynergyDetailSection` prefers `benefit_short` over dense `synergy_mechanism`.
- New `ManufacturerViolationsSection` widget renders `brand_trust_summary` with severity-matched styling.
- All 7 Flutter reference-data assets schema-synced with pipeline.
- 478 Flutter tests pass.

### Pipeline side — case closed

- All 6 validators (`--banned-recalled-only`, `--interaction-rules-only`, `--depletions-only`, `--harmful-additives-only`, `--synergy-only`, `--violations-only`) + `--strict` mode.
- Enricher passes through all authored fields.
- `build_final_db.py` emits all authored fields in blobs.
- Dashboard Clinical Copy view + staleness alerts + spot-check picker.
- 3,963 Python tests pass (including 6 new release-gate tests against production data).

---

## What's deferred for future work — Phase 1 through 5

### The roadmap in one picture

| Phase | Name                                 | Trigger / When                            | Effort      | Cost      | Status    |
| ----- | ------------------------------------ | ----------------------------------------- | ----------- | --------- | --------- |
| 0     | Stage 0 audit (this handoff)         | Already done                              | —           | —         | ✅ Done   |
| 1     | Pipeline runs in CI from cloud storage | **Next up — recommended start**         | 2–3 weeks   | $0/mo     | ⏸️ Future |
| 1.5   | **Safety Alert Short Path** (FDA recalls <15 min → user) | **Ship before public beta**     | 2–3 weeks   | $0/mo     | ⏸️ Future |
| 2     | Dr. Pham web editor (PR via UI)      | Nice-to-have for beta                    | 3–4 weeks   | $0/mo     | ⏸️ Future |
| 3     | Scheduled monthly DSLD category delta | V1 phase                                 | 2 months    | $0/mo     | ⏸️ Future |
| 4     | Reference-data hot-refresh           | V1 phase                                 | 2–3 months  | $0/mo     | ⏸️ Future |
| 4.5   | **Tiered offline architecture** (Yuka-style) | **Triggered at 50k products**         | 4–6 weeks   | $0/mo     | ⏸️ Future |
| 5     | Full observability / staging / audit | Ongoing from V1 onward                    | ongoing     | $0–$25/mo | ⏸️ Future |

### Phase 1 — pipeline in CI (next agent starts here)

**Where to find everything:**

- Full phase plan: `docs/AUTOMATION_ROADMAP.md` § "Phase 1 — Pipeline runs without your laptop"
- Cadence & ownership: `docs/PIPELINE_MAINTENANCE_SCHEDULE.md`
- Playbooks: `docs/PIPELINE_OPERATIONS_README.md` § "Playbooks"
- Technical command reference: `scripts/PIPELINE_OPERATIONS_README.md` (includes updated validator + schema version sections from today)

**Deliverables:**

- `.github/workflows/build-db.yml` — triggered on push to main + weekly cron.
- `config/brands.json` + `config/categories.json` — list of brands to build, category form codes.
- Cloud storage abstraction (Cloudflare R2 recommended — 10 GB free + zero egress).
- `scripts/storage_client.py` — reads raw JSON from R2 or local, interchangeable.
- GitHub Secrets: `DSLD_API_KEY`, `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, `PUBMED_API_KEY`, `UMLS_API_KEY`, `OPENFDA_API_KEY`, R2 credentials.

**Infrastructure to plumb during Phase 1 (don't gate anything yet, build the scaffolding):**

1. **Usage telemetry** → `usage_events` table in Supabase. Log scan count, stack size, feature-area activity per user. Data fuels Month 6 free-tier limit calibration.
2. **Tier-aware client scaffolding** → `auth.users.tier` metadata (`free` | `pro`). During beta everyone is `tier='pro'`; flipping the paywall switch at Month 6 is a one-line config change, not a refactor.

**First 2 weeks (from my recommendation):**

- Day 1: create `config/brands.json` + `config/categories.json`, commit.
- Day 2–3: Cloudflare R2 bucket setup, upload one brand as proof-of-concept.
- Day 4–5: draft `build-db.yml` workflow for the one brand; confirm it runs green.
- Week 2: scale to all 15 brands + Slack completion webhook.

### Phase 1.5 — Safety Alert Short Path

**Why this matters for next agent:** this is a safety-critical fast lane that ships in parallel with Phase 1. Must be built before public beta launch. FDA publishes a Class I recall → user's phone buzzes within 15 minutes. Not optional for a clinical app.

**Where to find:**

- Full architecture: `docs/AUTOMATION_ROADMAP.md` § "Phase 1.5 — Safety Alert Short Path"
- Operational playbooks (what to do when it fires / breaks): `docs/PIPELINE_OPERATIONS_README.md` § "Playbook 7 — Critical FDA recall just fired" / "Playbook 8 — Safety-alert poller is down"
- Role definitions: `docs/PIPELINE_MAINTENANCE_SCHEDULE.md` § "Safety-alert watcher" + "Safety-alert refiner"

**Key architectural decisions already made:**

- **15-minute polling cadence** via Cloudflare Cron Worker (free: 100k req/day, we use ~100/day).
- **Auto-drafted conservative copy** template engine (deterministic, not LLM) → ships in <15 min with safe default phrasing.
- **Dr. Pham refinement** within 24–48 hrs via the dashboard's safety-alerts queue — updates propagate via Supabase Realtime.
- **Client-side stack match** (privacy-preserving — server never sees user product stacks).
- **Three tiers:** CRITICAL (push notification), HIGH (in-app banner), CATALOG (silent refresh).
- **CRITICAL safety alerts never Pro-gated** — ethical + liability constraint documented in roadmap.

**Files to create:**

- `workers/safety-alert-poller.ts` — Cloudflare Worker.
- `supabase/functions/draft-safety-alert/index.ts` — Edge Function.
- `supabase/migrations/XXXX_safety_alerts.sql` — table creation.
- Flutter subscription handler + FCM push integration.

### Phase 4.5 — Tiered offline architecture

**When to trigger:** product catalog crosses 50k entries, OR Flutter app bundle exceeds 60 MB. Not now — beta's 10k fits fine in the current ~32 MB bundle.

**Why this matters:** the scale trajectory is 10k → 50k → 250k (all DSLD). A naive 250k local DB would be ~575 MB — app-store-unshippable. Research during the session found Yuka (6M products) ships only 1.6% locally; we should adopt the same pattern.

**Where to find:**

- Full architecture + Yuka/MyFitnessPal/Cronometer research: `docs/AUTOMATION_ROADMAP.md` § "Phase 4.5 — Tiered Offline Architecture"
- Scale math (sqlite-zstd gives 6.5x compression; 250k × 150 bytes/row ≈ 15 MB): same section
- Pro-tier monetization layer: same section § "Monetization layer — Pro-tier offline gating"

**Architecture decisions already made:**

- **Three tiers:** Tier 1 (50k in app binary, ~3 MB) → Tier 2 (150k post-install pack via iOS ODR / Android Play Asset Delivery, ~15 MB) → Tier 3 (all 250k + blobs on Supabase).
- **Pro gating:** Free tier gets Tier 1 + online Tier 3. Pro adds Tier 2 offline pack.
- **Snapshot + delta sync** via HTTP Range requests (NOT Supabase Realtime for product data, NOT PowerSync subscription).
- **New pipeline stage `build_verdict_shard.py`** to emit the compressed shards.

### Usage telemetry + tier scaffolding

**Where to find:** `docs/AUTOMATION_ROADMAP.md` § "Infrastructure consequences" (inside "Monetization timeline & free-tier limits")

**Build during Phase 1, don't enforce until Month 6:**

1. Supabase `usage_events` table. Every scan, stack edit, feature-area activity gets logged. Lets us calibrate the "free tier = N scans/day" limit from real data.
2. Flutter `currentUser.tier` check before every gate-able action. During beta, `tier` is always `'pro'` so nothing restricts; Month 6 the paywall flips with a config change.

**Free tier gates planned** (final values calibrated from beta telemetry):

| Feature                    | Free             | Pro           |
| -------------------------- | ---------------- | ------------- |
| Daily scans                | ~10/day (TBD)    | Unlimited     |
| Stack size                 | 5 products (TBD) | Unlimited     |
| Offline product DB         | Top 50k (Tier 1) | Top 150k (Tier 2) |
| Detail blob cache          | ~50 rolling      | Unlimited     |
| CRITICAL safety alerts     | **Always on**    | Always on     |
| HIGH/CATALOG safety alerts | Not included     | Included      |
| Alert history              | Current session  | Full export   |
| Family sharing             | Single user      | 5 members     |

---

## Files Created / Modified This Session (Complete Manifest)

### Pipeline data files (authored content)

- `scripts/data/medication_depletions.json` — 68 entries × 4–5 fields
- `scripts/data/banned_recalled_ingredients.json` — 143 entries × 3 fields + 9 reclassifications to `contamination_recall`
- `scripts/data/ingredient_interaction_rules.json` — 413 surfaces × 3 fields across severe / PL / non-severe
- `scripts/data/harmful_additives.json` — 115 entries × 2 fields
- `scripts/data/synergy_cluster.json` — 58 entries × 1 field (`synergy_benefit_short`)
- `scripts/data/manufacturer_violations.json` — 79 entries × 1 field (`brand_trust_summary`)

### Pipeline code

- `scripts/validate_safety_copy.py` — 6 validators + 6 CLI flags + Path B canonical_id retirement
- `scripts/enrich_supplements_v3.py` — authored-field passthrough fixes (condition_hits, drug_hits, pregnancy_block, synergy, manufacturer_violations)
- `scripts/build_final_db.py` — authored-field emission (safety_summary, benefit_short)
- `scripts/tests/test_safety_copy_production.py` — 6 release-gate tests (new file)

### Dashboard

- `scripts/dashboard/views/safety_copy.py` — Clinical Copy coverage view (new file)
- `scripts/dashboard/views/inspector.py` — authored-field rendering
- `scripts/dashboard/navigation.py` — nav entry for Clinical Copy
- `scripts/dashboard/app.py` — route wire-up
- `scripts/dashboard/page_meta.py` — page meta entry
- `scripts/dashboard/views/__init__.py` — export

### Flutter

- `lib/features/product_detail/widgets/interaction_warnings.dart` — normalize 4 authored-field families
- `lib/features/product_detail/widgets/pipeline_sections/synergy_detail_section.dart` — prefer `benefit_short`
- `lib/features/product_detail/widgets/pipeline_sections/manufacturer_violations_section.dart` — new widget (new file)
- `lib/features/product_detail/widgets/pipeline_detail_sections.dart` — barrel export
- `lib/features/product_detail/product_detail_screen.dart` — `_DeepDiveSection` wiring
- `assets/reference_data/banned_recalled_ingredients.json` — 139 → 143 entries
- `assets/reference_data/synergy_cluster.json` — schema 1.1 → 5.0.0 with `synergy_benefit_short` patched in
- `assets/reference_data/medication_depletions.json` — synced in prior session

### Documentation

- `docs/AUTOMATION_ROADMAP.md` — 6-month phased plan, free-tier stack, monetization timeline (new file)
- `docs/PIPELINE_MAINTENANCE_SCHEDULE.md` — cadence + roles + 4 flywheels (new file)
- `docs/PIPELINE_OPERATIONS_README.md` — 5-stage funnel + 9 playbooks + SOPs (new file)
- `docs/HANDOFF_2026-04-18.md` — this document (new file)
- `scripts/PIPELINE_OPERATIONS_README.md` — updated with validator CLI section + reference-data schema versions + linter whitespace fixes
- `scripts/safety_copy_exemplars/` — multiple instruction docs for Dr. Pham rounds
- `scripts/safety_copy_exemplars/round_2_pregnancy_lactation_shell.json` — shell workspace
- `scripts/safety_copy_exemplars/round_2b_full_shell.json` — shell workspace
- `scripts/safety_copy_exemplars/ROUND_2_PREGNANCY_LACTATION_INSTRUCTIONS.md`
- `scripts/safety_copy_exemplars/ROUND_2B_FULL_INSTRUCTIONS.md`
- `scripts/safety_copy_exemplars/ADR_contamination_recall_ban_context.md` — ADR implemented

---

## Recommendations & Advice for Next Agent

### Before touching Phase 1

1. **Read in this order** (top-down for context, bottom-up for tactics):
   - `docs/AUTOMATION_ROADMAP.md` — the vision
   - `docs/PIPELINE_OPERATIONS_README.md` — the op playbooks
   - `docs/PIPELINE_MAINTENANCE_SCHEDULE.md` — who-does-what
   - `scripts/PIPELINE_OPERATIONS_README.md` — actual commands
2. **Run the dashboard** once to see the Clinical Copy coverage view and understand what's green.
3. **Run the full test suite** (`python3 -m pytest scripts/tests/ -q`) — 3,963 should pass. This is your "known-green" baseline.

### When building Phase 1

- **Start tiny.** One brand, not all 15. Prove the CI path for Nature_Made, then scale.
- **Don't touch the pipeline code.** Phase 1 is pure orchestration — YAML + storage glue. If you find yourself modifying `run_pipeline.py` or `build_final_db.py`, back up and reconsider.
- **Prefer Cloudflare R2** for storage. 10 GB free + zero egress cost = the strongest free tier available.
- **GitHub Actions private-repo free tier** is 2,000 min/month. A full build is ~20 minutes. That's 100 builds/month budget — plenty for weekly cron + PR validations. Switch to public repo or GitHub Pro only if you exceed it.

### When building Phase 1.5

- **Don't use Supabase Realtime for product-catalog sync.** Only for `safety_alerts` table. Product catalog uses snapshot+delta sync (Phase 4.5).
- **Auto-drafted copy is a template engine, not an LLM.** Deterministic. Testable. Write the template rules in a single file (`supabase/functions/draft-safety-alert/copy_template.ts` or similar).
- **Safety alerts never Pro-gate Tier 1 CRITICAL.** The code path must not check `user.tier` on those notifications.

### Things to NOT do

- Don't retrofit Pro-tier gating after the fact. Build `currentUser.tier` checks into every gate-able action from Phase 1, even though beta doesn't enforce.
- Don't ship Phase 1.5 as "we'll add it later" — FDA recall delay is a clinical-trust failure.
- Don't skip the `validate_safety_copy.py --strict` check in CI. It's the release gate for Dr. Pham's work.
- Don't build a flat local DB with 250k rows. Re-read Phase 4.5 before changing the Flutter data model.
- Don't lower-case the uppercase canonical_ids (`BANNED_*`, `RECALLED_*`, etc.). That was formally retired as migration debt in Path B — they're the intentional convention now.

### Monitoring during Phase 1 rollout

- **Watch GitHub Actions for the first 10 builds** before trusting the cron. Failure modes: timeouts, OOM (big enriched JSONs), Supabase rate-limits.
- **Daily-check the Clinical Copy dashboard** for the first week after Phase 1 launches. If the pipeline is breaking the authored fields somehow, coverage badges drop to red and you catch it immediately.

---

## Operational context for tonight's sequence

If Sean is executing tonight (which he is):

1. Commit and push this handoff + ops README + production tests.
2. Run Supabase sync (`scripts/sync_to_supabase.py` with `--cleanup` if confirming clean upload).
3. Run Flutter bridge (`scripts/import_catalog_artifact.sh`) to update the bundled DB.
4. Commit Flutter bundle update.

Commands are documented in `scripts/PIPELINE_OPERATIONS_README.md` (updated this session).

After tonight: Sean rests. Next agent picks up Phase 1 with this handoff + the 3 doc files as the starting point.

---

## Contact points

- **Pipeline operator:** Sean
- **Clinical author:** Dr. Pham (round 1 + 2a + 2b-full complete)
- **Repos:** `github.com/seancheick/PharmaGuide_Pipeline` (pipeline) + `github.com/seancheick/Pharmaguide.ai` (Flutter)
- **Supabase project:** see `.env` (via `scripts/env_loader.py`)

Good luck. The foundation is clean.
